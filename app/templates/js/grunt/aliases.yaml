lint:
    - 'newer:jshint:extension'

compile:
    - 'lint'

config:
    - 'bower'

build:
    - 'compile'
    - 'config'
    - 'requirejs'
    - 'uglify'

release:
    - 'build'
    - 'copy'

test:
    - 'newer:jshint:extension'
    - 'if-missing:curl-dir:jasmine'
    - 'connect:test'

