lint:
    - 'jshint:extension'

compile:
    - 'lint'

config:
    - 'bower'

build:
    - 'compile'
    - 'config'
    - 'requirejs'
    - 'uglify'

release:
    - 'build'
    - 'copy'

test:
    - 'jshint:extension'
    - 'if-missing:curl-dir:jasmine'
    - 'connect:test'

